# Training loop hyperparameters (model-agnostic).
epochs: 40
batch_size: 64
lr: 0.001
weight_decay: 0.00001
early_stop_patience: 8
early_stop_min_delta: 0.0001
grad_clip: 1.0
num_workers: 0
device: auto            # auto | cpu | cuda
ckpt_dir: checkpoints
log_every: 10
seed: 0
